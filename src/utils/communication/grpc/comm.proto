syntax = "proto3";

service CommunicationServer {
  rpc SendData (Data) returns (Empty) {}
}

message Empty {}

message Data {
  string id = 1;
  Model model = 2;
}

message ID {
  int32 num = 1;
  string id = 2;
}
